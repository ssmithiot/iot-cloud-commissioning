# Edge Release 0.1.6

Status: Superseded fleet baseline

The working Edge UI/agent baseline verified on GW008 and the existing fleet
before 0.1.7.  Agent tag: `edge-agent-v0.1.6` at cloud commit `8748dd2`.

Rollback: use the previously validated fleet code-only artifact.  This record
exists so that 0.1.7 and later releases always name an explicit base.
