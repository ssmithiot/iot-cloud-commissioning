# Edge Release 0.1.6

Status: Superseded fleet baseline

Base release: pre-release fleet baseline

## Scope

- Edge UI and agent fleet baseline before 0.1.7.
- Gateway/site data: preserved.

## Immutable source

- Agent tag: `edge-agent-v0.1.6` at cloud commit `8748dd2`.
- Edge artifact: the validated fleet 0.1.6 code-only artifact.

## Validation and rollback

GW008 and the fleet supplied the working RPM/read-write baseline.  Roll back
to the preceding validated fleet artifact when needed.  This record ensures
that 0.1.7 and later releases always name an explicit base.
