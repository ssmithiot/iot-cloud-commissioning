# Edge 0.2.0 — Validation Evidence

Date: 2026-07-31, updated 2026-08-03 for RC2. Workstation: `IOT-AI-01 Linux`.

## Status

| Gate | State |
|---|---|
| Local test suites | Passed |
| Release artifact and manifest validation | Passed |
| Release ledger check | Passed |
| GW006 pilot | **Not run — blocked, no usable credential** |

This candidate must not be deployed to any gateway until the GW006 pilot below
is completed.

## RC2 — Trend Viewer correction (2026-08-03)

RC1 was tagged `edge-ui-v0.2.0-rc1` / `edge-agent-v0.2.0-rc1`. Those tags are
immutable and were not moved, deleted or overwritten. RC2 supersedes RC1 and
changes the **Edge UI only**; the agent payload is byte-identical between the
two candidates.

Five viewer defects were found and corrected before the pilot: the Trend Groups
action opened a table view rather than the graph; table and graph existed as
separate viewer modes although the graph page already carried a sample table;
saved-view settings reset on every range change; the viewer requested 50,000
rows against a query path that clamped to the newest 2,000, so 2-day and 7-day
ranges showed only their tail; and a permanent side panel consumed chart width.
Both the 2-day and 7-day controls were retained — the query and display path was
fixed instead. See `docs/releases/edge/0.2.0.md` §"Trend Viewer correction
(RC2)".

No BACnet sampling, lock, port, router, collection-interval, cloud-ownership or
upload behaviour was changed. No stored sample was deleted, shortened or
rewritten; both the graph and table bounds are display limits over data that
stays in SQLite.

| | RC1 | RC2 |
|---|---|---|
| Edge UI commit | `e504879fda1f408c2f9c97d6ada55de8d105230a` | `cd4c0a5468c6d6d8937de62b6ddcc1119bd17d6e` |
| Artifact SHA-256 | `1e634fe2a58bdad7e21827e4a4e706c0c3240a739a378bb7e0b6bdc307f95bad` | `78fac819890a53ae30050fbfdc40fafe470ed2d04460e4d1485416a4c57083f9` |
| Agent source commit | `40133f2a81390db92a01b33a9c02c48a07363a7e` | `40133f2a81390db92a01b33a9c02c48a07363a7e` (unchanged) |
| Edge UI tests | 138 passed | 171 passed |

### RC2 behaviour evidence

Measured on a rendered viewer page holding seven days of real-shaped history —
one point at 60-second intervals (10,080 raw rows) and one at 600-second
intervals (1,008 raw rows), 24-hour and 7-day requests:

| Observation | Result |
|---|---|
| 7-day request, fast point | 1,364 plotted values spanning 7.00 days |
| 7-day request, slow point | 1,008 plotted values spanning 6.99 days (under target, returned whole) |
| Recent-samples table, same page | 500 rows, bounded independently of the graph |
| Stored `trend_samples` rows after viewing every range | unchanged |

Chart layout was checked by executing the page's own JavaScript under a DOM
stub. With one Y axis in use the plot area is 1,460 px of the 1,600 px canvas;
with two axis groups it is 1,378 px. RC1 drew 1,215 px *and* gave up a further
320 px of the row to the fixed side panel.

## Repositories

| | Edge UI | Cloud / Edge Agent |
|---|---|---|
| Path | `/home/steve/projects/iot/edge-bacnet-ui` | `/home/steve/projects/iot/iot-cloud-commissioning` |
| Remote | `github.com/ssmithiot/edge-bacnet-commissioning-ui` | `github.com/ssmithiot/iot-cloud-commissioning` |
| Branch created | `release/edge-ui-0.2.0` | `release/edge-agent-0.2.0` |
| Branched from | `release/edge-ui-0.1.9` @ `719d4a82ed972269d44db7c0638800b26e82002d` (tag `edge-ui-v0.1.9-final`) | `release/edge-agent-0.1.9` @ `e634e7704aa61a43245c0d10a81bbc237bef976f` |

Agent payload equivalence to the authoritative deployed baseline was verified
by tree hash, not by inspection:

```
git rev-parse d2722d39:edge-agent  -> fdea438fcf435bce6b4128697d3401a295c0b7da
git rev-parse e634e770:edge-agent  -> fdea438fcf435bce6b4128697d3401a295c0b7da
```

`main` was not used in either repository. Edge UI `main` (`3949ece`) shares no
common ancestor with the release line; cloud `main` (`66c5f33`) does not contain
the authoritative agent commit and still declares agent `0.1.6`.

## Test results

### Edge UI — `edge-bacnet-ui`

| Run | Result |
|---|---|
| Baseline, before any change | **111 passed** |
| RC1, after 0.2.0 | **138 passed, 0 failed** |
| RC2, after the Trend Viewer correction | **171 passed, 0 failed** |

Command: `./.venv/bin/python -m pytest tests -q`

27 tests added for RC1 and a further 33 for RC2, 60 in total. No test was
deleted, skipped, or marked expected-failure. Two RC1 tests that named the
removed standalone table mode (`test_table_view_shows_samples`,
`test_table_view_shows_failed_reads_as_errors_not_values`) were renamed to
`test_recent_samples_table_*` and now assert the same sample and error content
on the graph page, which is where that table lives.

### Cloud repository — `iot-cloud-commissioning`

Covers `cloud-api/tests`, `edge-agent/tests` and `tools/tests`.

| Run | Result |
|---|---|
| Baseline, before any change | 529 passed, **5 failed** |
| Final, after 0.2.0 | **560 passed, 1 failed** |

Command: `python -m pytest -q` from the repository root.

31 tests added.

#### The 5 baseline failures

Four were pre-existing test-order dependencies, all passing in isolation and
failing in a full run:

- `edge-agent/tests/test_trends.py::test_sampling_logs_safe_route_diagnostics`
- `edge-agent/tests/test_trends.py::test_local_sampler_not_invoked_when_flag_is_absent`
- `edge-agent/tests/test_tunnel.py::test_handle_tunnel_message_proxies_to_local_ui`
- `edge-agent/tests/test_tunnel.py::test_handle_tunnel_message_logs_redirect_shape_without_location_value`

All four are fixed, by fixing the two underlying defects rather than the tests:

1. `cloud-api/alembic/env.py` called `fileConfig()` with the default
   `disable_existing_loggers=True`. Any in-process migration — the release
   gate, and `test_uuid_schema_alignment` / `test_legacy_schema_reconciliation`
   — silently switched off application logging for everything that ran
   afterwards. Verified by bisection: those two files, and only those two,
   reproduced the failure.
2. `iot_cx_agent/main.py` held a module-level "already logged" latch that
   leaked between tests. It was removed as part of the trend-step refactor.

#### The 1 remaining failure

`cloud-api/tests/test_api.py::test_gateway_workspace_contains_discovery_progress_ui`

Fails on `assert "Remove device" in response.text`. This is **pre-existing and
not introduced by 0.2.0**. Evidence:

```
git show d2722d39:cloud-api/app/ui.py    | grep -c "Remove device"  -> 0
git show e634e770:cloud-api/app/ui.py    | grep -c "Remove device"  -> 0
git show d2722d39:cloud-api/tests/test_api.py | grep -c '"Remove device" in response.text' -> 1
```

The assertion is present, and the feature it asserts is absent, at the
authoritative deployed 0.1.9 baseline itself. Fixing it means restoring a
removed Cloud UI control, which is out of scope for this release. It was not
skipped, weakened, or deleted. Recorded as a deferred item below.

### Release tooling

`tools/tests`: **147 passed** at RC1 and again at RC2, including manifest
validation, updater package inventory, Rule #1 validation, and the release
ledger check.

```
python tools/validate_release_manifest.py tools/releases/manifests/edge-0.2.0.json
Edge Release 0.2.0: base=0.1.9, rollback=0.1.9
Artifact verified: tools/releases/gw006-edge-ui-0.2.0-code.tar.gz
```

RC2 rebuilt the artifact, so `FINAL_UI_COMMIT` and `FINAL_UI_ARTIFACT_SHA256` in
`tools/tests/test_legacy_edge_upgrade_webapp.py` were updated to the RC2 values.
Four tests assert against those constants
(`test_authoritative_release_definition_supplies_ui_agent_and_trend_policy`,
`test_embedded_artifact_summary_validates_release_checksum`,
`test_preflight_summary_reports_embedded_artifact_release_fields`,
`test_build_upload_zip_dry_run_reports_embedded_artifact_without_mutation`);
they failed against the stale constants first, which is the check working. No
assertion was weakened and no test was skipped.

The `cloud-api` and `edge-agent` suites were **not rerun for RC2** and their RC1
results above still stand: RC2 touches no file either suite covers. The change
is confined to the Edge UI repository plus this repository's release manifest,
notes, validation record and the two tooling constants above.

## Coverage of the required 0.2.0 test list

| Required | Where |
|---|---|
| Trends disabled by default in 0.1.9 authority | Preserved as history; 0.2.0 identity asserted in `test_config_defaults_local_edge_trends_to_enabled_when_absent` and `test_trends_are_enabled_by_default_in_this_release` |
| Trends explicitly enabled in 0.2.0 | `test_trends_are_enabled_by_default_in_this_release`, `test_local_sampler_runs_when_flag_is_absent_from_config` |
| UI routes available in 0.2.0 | `test_trend_routes_are_available` |
| Existing historical database survives upgrade | `test_upgrade_preserves_an_existing_trend_database`, `test_upgrade_adds_missing_tables_and_columns`, `test_upgrade_backs_up_the_database_before_changing_it`, `test_upgrade_is_idempotent_and_reruns_take_no_backup`, `test_upgraded_database_accepts_new_samples` |
| Group create / update / enable / disable / delete | `test_group_can_be_created_with_saved_points`, `test_group_points_and_interval_can_be_updated`, `test_group_can_be_disabled_and_re_enabled`, `test_group_is_deleted_only_by_an_explicit_delete_action` |
| Interval due / not-due behaviour | `test_group_not_yet_due_issues_no_bacnet_command`, `test_group_stays_due_after_deferring_to_live_work` |
| New group discovered on next agent cycle | `test_group_added_after_startup_is_discovered_on_next_cycle` |
| Good, missing and failed BACnet reads | `test_local_sampler_persists_good_missing_and_error_samples_and_outbox`, `test_recent_samples_table_shows_failed_reads_as_errors_not_values` |
| Table viewer | `test_recent_samples_table_shows_samples` |
| Graph viewer | `test_graph_view_plots_numeric_samples` |
| Binary and multistate parsing | `test_graph_view_plots_binary_and_multistate_samples` |
| Agent restart | `test_backlog_survives_an_agent_restart` |
| UI restart | `test_group_survives_a_ui_restart` |
| Cloud unavailable | `test_collection_continues_while_the_cloud_is_unreachable`, `test_local_sampling_does_not_depend_on_cloud_trend_configs` |
| Upload retry and backfill | `test_cloud_outage_keeps_the_sample_and_schedules_a_retry`, `test_backlog_is_uploaded_when_the_cloud_returns` |
| Duplicate upload rejection / idempotency | `test_nothing_is_uploaded_twice`, `test_a_retry_resends_the_same_event_ids`, `test_resending_the_same_event_id_is_ignored`, `test_a_batch_with_duplicate_event_ids_is_rejected` |
| Bounded batch behaviour | `test_trend_reads_are_split_into_bounded_batches`, `test_per_cycle_point_budget_is_enforced`, `test_upload_batch_is_bounded` |
| Operational read/write priority over trend work | `test_trend_reads_give_up_the_runtime_lock_quickly`, `test_no_bacnet_command_runs_while_an_operator_holds_the_runtime`, `test_trend_reads_yield_between_batches_when_live_work_appears`, `test_busy_runtime_mid_run_defers_without_writing_samples` |
| Existing BACnet read/write tests unchanged and passing | `edge-agent/tests/test_bacnet.py`, `test_bacnet_read.py`, `test_local_write.py` — unmodified, passing |

### RC2 required test list

All in `tests/test_edge_trends_enabled.py` unless noted.

| Required | Where |
|---|---|
| Trend Groups displays "View", not "View samples" | `test_trend_groups_offers_view_not_view_samples` |
| View opens the graph directly | `test_the_view_action_opens_the_graph_directly` |
| No standalone table-mode navigation | `test_there_is_no_standalone_table_mode_navigation` |
| Sample table remains below the graph | `test_the_sample_table_stays_below_the_graph`, `test_the_points_and_axes_panel_is_above_the_chart` |
| Saving a new view redirects with its view ID | `test_saving_a_new_view_redirects_with_its_view_id` |
| Updating an existing view keeps the same view ID | `test_updating_an_existing_view_keeps_the_same_view_id` |
| Every standard range preserves the selected view | `test_every_standard_range_preserves_the_selected_view` (5 cases) |
| Custom hours preserves the selected view | `test_custom_hours_preserves_the_selected_view`, `test_saving_preserves_the_current_range_and_custom_hours` |
| Point visibility survives every range change | `test_point_visibility_survives_every_range_change` (6 cases) |
| Y1/Y2/Y3/Binary assignments survive every range change | `test_axis_assignments_survive_every_range_change` (6 cases), `test_a_saved_view_covers_points_with_no_samples_in_the_range` |
| A dataset over 2,000 rows displays the complete requested span | `test_a_dataset_larger_than_the_old_row_limit_still_spans_the_request` |
| Seven-day data starts near the boundary and reaches the newest sample | `test_seven_day_data_spans_seven_days` |
| Multiple points all represented past the plotting target | `test_one_busy_point_cannot_hide_the_others` |
| Downsampling preserves first, last, minimum and maximum | `test_downsampling_preserves_first_last_minimum_and_maximum` |
| Recent-samples table bounded independently of graph data | `test_graph_and_table_budgets_are_independent` |
| Existing trend history unchanged | `test_viewing_never_changes_stored_history`, plus the RC1 upgrade-safety tests, all unmodified and passing |

## Release artifact

| Field | Value |
|---|---|
| Artifact | `tools/releases/gw006-edge-ui-0.2.0-code.tar.gz` |
| SHA-256 (RC2) | `78fac819890a53ae30050fbfdc40fafe470ed2d04460e4d1485416a4c57083f9` |
| SHA-256 (RC1, superseded) | `1e634fe2a58bdad7e21827e4a4e706c0c3240a739a378bb7e0b6bdc307f95bad` |
| Entries | 44 |
| Built from | Edge UI `cd4c0a5468c6d6d8937de62b6ddcc1119bd17d6e` |
| Builder | `tools/build_edge_ui_artifact.py` |

The build is deterministic — sorted entries, fixed timestamps, no uid/gid or
user names — and was rebuilt to confirm the same SHA-256. The archive contains
runtime code only; `data/`, `.env`, `start.sh`, `.git`, `.local-backups`,
`__pycache__`, and any `.db`/`.sqlite`/`.pyc` are excluded, and the updater's
`validate_embedded_ui_artifact_contents` check enforces this.

For RC2 the rebuild was verified three ways: a second build to a separate output
path produced the same digest; `app.py`, `edge_trend_store.py`,
`templates/edge_trend_viewer.html` and `templates/edge_trends.html` extracted
from the archive hash-match the same paths at Edge UI commit `cd4c0a5`; and the
archive contains no `data/`, `.env`, `.git`, `.local-backups`, `__pycache__` or
`.db`/`.sqlite`/`.pyc` entry.

```
python tools/validate_release_manifest.py tools/releases/manifests/edge-0.2.0.json
Edge Release 0.2.0: base=0.1.9, rollback=0.1.9
Artifact verified: tools/releases/gw006-edge-ui-0.2.0-code.tar.gz
```

## Baseline preservation

Full `--all` git bundles of both repositories were taken before any change, and
no branch, tag, or history was rewritten or force-pushed. The local-only branch
`docs/authority-0.1.9-baseline` (`4969cab`) was left untouched; note it is still
unpushed and exists only on this workstation.

## GW006 pilot — blocked

**The pilot was not run and no gateway was contacted.** No file, service, or
configuration on GW006 or any other gateway was read or modified. GW015 was not
contacted.

What was established about reachability, from this workstation:

| Check | Result |
|---|---|
| Tailnet membership | 3 nodes (`iot-ai-01-linux`, `iot-ai-01`, `msi`). No gateway. |
| `ping 10.2.0.15` (site Cradlepoint WAN) | Reachable |
| `ping 192.168.1.200`, `192.168.1.102` (gateway-side LAN) | No route — not routable from here |
| `10.2.0.15` TCP 22 / 80 / 443 | Open |
| SSH with `~/.ssh/gw006_codex_ed25519` as `swadmin`, `BMS_admin`, `admin` on ports 22 / 2222 / 47822 | `Permission denied (publickey,password)` |

The gateway sits behind the Cradlepoint on a LAN this workstation cannot route
to. The documented update path is the Legacy Updater webapp run from the
Windows machine, which authenticates with a Cradlepoint password and a gateway
password. Neither is present on this workstation, in the environment, or in
either repository. No further credential attempts were made.

### Remaining pilot work

Pre-install capture:

1. `systemctl status edge-bacnet-ui iot-cx-agent`
2. Installed UI and agent versions and commits
3. Back up the Edge UI code and `data/`
4. Back up `/etc/iot-cx-agent/agent.yaml` and `/var/lib/iot-cx-agent/edge.db`
5. Row counts from `data/edge-trends.db` for `trend_groups`, `trend_points`,
   `trend_runs`, `trend_samples`, `trend_upload_outbox`
6. Current BACnet port ownership (`ss -lunp | grep 478`)
7. Confirm `/home/swadmin/gw-recovery/0.2.0/` checkpoint and restore commands

Acceptance, using one small representative group first, run long enough to
prove several real intervals plus restart and outage recovery:

1. Existing devices and points intact
2. Existing historical trends intact
3. A 60-second group collects repeatedly
4. Viewer updates with real values
5. Cloud receives the same samples
6. Disconnecting the cloud does not stop local collection
7. Reconnecting uploads the backlog
8. Restarting the Edge UI loses no configuration
9. Restarting the agent loses no configuration or backlog
10. Live BACnet/IP refresh stays under 1 second
11. MS/TP refresh stays around 3 seconds
12. Single read and write stay responsive during collection
13. No duplicate samples
14. No uncontrolled backlog growth
15. No repeating exceptions or rapid retry loops in the logs

## Deferred items

Found during this work, deliberately not addressed here.

1. **`test_gateway_workspace_contains_discovery_progress_ui` fails at the
   0.1.9 baseline.** The workspace UI asserts a "Remove device" control that
   does not exist in `cloud-api/app/ui.py`. Either the control was dropped
   without updating the test, or it was never wired up. Needs a decision on
   which side is correct.
2. **OPEN-03 inheritance.** `release/edge-agent-0.2.0` carries the 16
   post-baseline maintenance commits whose cloud-side approval is recorded as
   undecided. The agent payload is unaffected, but the cloud-side changes now
   ride into 0.2.0 and should be approved explicitly.
3. **`docs/authority-0.1.9-baseline` is unpushed** and exists on one disk only.
4. **`sample_configured_trends`** (the older cloud-configured trend path) still
   polls the cloud for configuration every cycle and coexists with the
   Edge-owned path. Whether it should be retired now that the Edge is
   authoritative is an open design question.
5. The Edge UI has **no automated coverage of the graph's client-side
   JavaScript**; graph tests assert the serialised series data only. RC2 checked
   the drawing code once by hand, executing it under a DOM stub to confirm the
   layout change, but that harness was not committed as a test and does not run
   in CI. Still open.
